# Production Environment Variables
# Copy this file to .env.production.local and fill in your values
# These should also be set in Vercel Dashboard → Settings → Environment Variables

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL="https://[your-project].supabase.co"
# New publishable key format (REQUIRED for client-side)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY="sb_publishable_your-publishable-key"
# Legacy anon key (DEPRECATED - fallback only)
# NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
# New secret key format (REQUIRED for server-side admin)
SUPABASE_SECRET_KEY="sb_secret_your-secret-key"
# Custom override for Vercel integration issues
# CUSTOM_SUPABASE_SERVICE_KEY="your-custom-service-key"
# Legacy service role key (DEPRECATED - disabled by Supabase)
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Database URLs (from Supabase Dashboard → Settings → Database)
# Pooled connection for app queries
POSTGRES_URL="postgresql://postgres.[project]:[password]@[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
# Direct connection for migrations
POSTGRES_URL_NON_POOLING="postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres"

# Site URL (your production domain)
NEXT_PUBLIC_SITE_URL="https://your-domain.vercel.app"

# Optional: Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1