# Supabase Configuration
# For local development: Get these from `npx supabase status` after running `npx supabase start`
# For production: Get these from your Supabase project dashboard

NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321

# Client-side key (MUST be prefixed with NEXT_PUBLIC_)
# Use EITHER the new publishable key OR legacy anon key (both work):
# - New format (June 2025+): sb_publishable_xxxxx
# - Legacy format (pre-2025): eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key-here

# Server-side admin key (MUST NOT be prefixed with NEXT_PUBLIC_)
# Use EITHER the new secret key OR legacy service_role key (both work):
# - New format (June 2025+): sb_secret_xxxxx
# - Legacy format (pre-2025): eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SECRET_KEY=sb_secret_your-secret-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Migration Guide:
# - Before Nov 2025: Both new (sb_secret_/sb_publishable_) and legacy keys work
# - After Nov 2025: Monthly reminders to migrate
# - Before late 2026: Must migrate to new key format
# - After late 2026: Legacy keys will be deleted

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Database URLs (for Prisma compatibility)
POSTGRES_URL=postgresql://postgres:postgres@localhost:54322/postgres
POSTGRES_URL_NON_POOLING=postgresql://postgres:postgres@localhost:54322/postgres

# OAuth Providers (optional)
GOOGLE_OAUTH_CLIENT_ID=your-google-client-id
GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret

# Legacy Auth.js (can be removed after full migration)
AUTH_URL=http://localhost:3000
AUTH_SECRET=generate-with-openssl-rand-base64-32